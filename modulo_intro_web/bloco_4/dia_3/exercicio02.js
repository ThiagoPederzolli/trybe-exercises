let n = 5;
let linhas = '';

//para cada loop adiciona um asterisco a string linhas e já imprime ela no console, ou seja, a cada loop é impressa uma string com um asterisco a mais que a anterior.
for (i = 0; i < n; i += 1){
  linhas += '*';
  console.log(linhas);
}
